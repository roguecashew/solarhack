"use client";

import { Card } from "@/components/ui/Card";

// PLACEHOLDER — Chat tab. Owned by workstream D (replace).
export default function ChatPage() {
  return (
    <Card>
      <p className="text-muted">Expanded Sentinel Assistant conversation.</p>
    </Card>
  );
}
