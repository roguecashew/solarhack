"use client";

import { Card } from "@/components/ui/Card";

// PLACEHOLDER — Reports tab. Owned by workstream E (replace).
export default function ReportsPage() {
  return (
    <Card>
      <p className="text-muted">Exportable memo / report view.</p>
    </Card>
  );
}
