"use client";

import { Card } from "@/components/ui/Card";

// PLACEHOLDER — Documents tab. Owned by workstream E (replace).
export default function DocumentsPage() {
  return (
    <Card>
      <p className="text-muted">Source documents uploaded for this project.</p>
    </Card>
  );
}
