"use client";

import { Card } from "@/components/ui/Card";

// PLACEHOLDER — Timeline tab. Owned by workstream E (replace).
export default function TimelinePage() {
  return (
    <Card>
      <p className="text-muted">Critical-path timeline with the Dec 31, 2030 deadline.</p>
    </Card>
  );
}
