"use client";

import { Card } from "@/components/ui/Card";

// PLACEHOLDER — Map tab. Owned by workstream E (replace).
export default function MapPage() {
  return (
    <Card>
      <p className="text-muted">
        Project-level GIS view — layered toggles and zone shading (placeholder).
      </p>
    </Card>
  );
}
